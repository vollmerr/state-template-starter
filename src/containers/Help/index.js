import React from 'react';

export default () => (
  <div>
    <h1>In Help Page...</h1>
    <p>By defualt, links such as the `Contact Us` link, point to this page.</p>
    <p>To change this, set a `contactLink` in the main StateTemplate container entry point where routes are set.</p>
  </div>
);
